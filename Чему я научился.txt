🧠 Чему я научился в проекте "CLI-калькулятор с автотестами"
🐍 Язык программирования: Python
✅ Работа с Python CLI-приложениями:
Создание консольного интерфейса с использованием argparse.

Обработка аргументов командной строки (ввод чисел и операций).

Организация структуры проекта: разбиение на модули (main.py, calculator.py, tests/).

Написание функций с docstring’ами и базовой валидацией входных данных.

Обработка исключений (деление на ноль и некорректный ввод).

✅ Автоматическое тестирование:
Написание юнит-тестов с использованием:

unittest (стандартная библиотека)

pytest (внешняя библиотека)

Проверка корректной работы арифметических операций: +, -, *, /.

Написание тестов на ошибки и исключения (ZeroDivisionError).

Структурирование тестов в отдельных файлах (test_pytest.py, test_unittest.py).

Изучение принципа TDD (сначала тест, потом код).

Запуск тестов через терминал (pytest, python -m unittest).

✅ Работа с окружением и зависимостями:
Создание и активация виртуального окружения (venv).

Установка зависимостей через pip install.

Создание файла requirements.txt.

Запуск и отладка проекта через консоль (VSCode / терминал).

Добавление README.md с описанием проекта и инструкциями по запуску.